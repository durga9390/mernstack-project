let check_Number=(a)=>{
    for(let i=0; i<10; i++){
    if(a>i){
        console.log(`${a} is big than ${i}`);  
    }
    else{
        console.log(`${a} is small than ${i}`);  
    }
    }
}
check_Number(5)